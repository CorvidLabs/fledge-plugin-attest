# Changelog

## 0.3.0

- Track `CorvidLabs/attest` `0.5.0`.
- Add `fledge attest forward` and `fledge attest keygen`.
- Add `--color auto|always|never` for `log` and `verify`.
- Mirror upstream policy, signing-key warning, and lenient log handling updates.

## 0.1.0

- Initial release
- `fledge attest sign` / `verify` / `log` / `export`, backed by `AttestKit`
